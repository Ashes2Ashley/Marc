# Optional absolute Worker URL when the SPA is NOT served from the same origin.
# Leave unset for same-origin /api (recommended with Cloudflare Workers + assets).
# VITE_WORKER_URL=https://marc-live.<your-subdomain>.workers.dev
